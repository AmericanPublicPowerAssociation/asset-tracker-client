import { connect } from 'react-redux'
import ExportsWindow from '../components/ExportsWindow'
import {
} from '../actions'
import {
} from '../selectors'


const mapStateToProps = state => ({
})


const mapDispatchToProps = dispatch => ({
})


export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(ExportsWindow)
