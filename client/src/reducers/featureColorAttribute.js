import { FEATURE_COLOR_ATTRIBUTE } from '../constants'

const initialState = FEATURE_COLOR_ATTRIBUTE

const featureColorAttribute = (state=initialState, action) => {
  return state
}

export default featureColorAttribute
