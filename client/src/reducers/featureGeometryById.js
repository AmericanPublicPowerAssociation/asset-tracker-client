import { FEATURE_GEOMETRY_BY_ID } from '../constants'

const initialState = FEATURE_GEOMETRY_BY_ID

const featureGeometryById = (state=initialState, action) => {
  return state
}

export default featureGeometryById
