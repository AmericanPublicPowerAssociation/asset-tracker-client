import { FEATURE_SIZE_ATTRIBUTE } from '../constants'

const initialState = FEATURE_SIZE_ATTRIBUTE

const featureSizeAttribute = (state=initialState, action) => {
  return state
}

export default featureSizeAttribute
