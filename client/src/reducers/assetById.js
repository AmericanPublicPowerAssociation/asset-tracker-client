import { ADD_ASSET } from '../constants'

const initialState = {
  'a': {'name': 'aaa'},
  'b': {'name': 'bbb'},
}

const assetById = (state=initialState, action) => {
  return state
}

export default assetById
